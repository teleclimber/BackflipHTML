{
  "name": "@backflip/html",
  "type": "module",
  "exports": "./dist/mod.js",
  "types": "./dist/mod.d.ts",
  "scripts": {
    "build": "npx tsc",
    "build:extension": "npm run build && cd lsp && npm run build && cd ../vscode-backflip && npm run build && vsce package --no-dependencies",
    "preview": "npx tsx preview/server.ts"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "acorn": "^8.16.0",
    "parse5-html-rewriting-stream": "^8.0.0"
  },
  "devDependencies": {
    "@types/jsdom": "^28.0.1",
    "jsdom": "^29.0.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
